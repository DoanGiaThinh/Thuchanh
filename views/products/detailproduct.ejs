<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chi Tiết Sản Phẩm - <%= products.ten %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
</head>
<body>
    <div class="container mt-4">
        <h1 class="text-center mb-4">Chi Tiết Sản Phẩm</h1>
        <div class="row">
            <!-- Hình ảnh sản phẩm -->
            <div class="col-md-6">
                <img src="<%= products.hinhanh %>" alt="<%= products.ten %>" class="img-fluid rounded shadow-sm">
            </div>
            <!-- Thông tin sản phẩm -->
            <div class="col-md-6">
                <h3><%= products.ten %></h3>
                <p><strong>Mã Sản Phẩm:</strong> <%= products.masp %></p>
                <p><strong>Giá:</strong> <%= products.gia %> VND</p>
                <p><strong>Mô Tả:</strong> <%= products.mota %></p>
                <p><strong>Nhóm:</strong> <%= products.idnhom %></p>

                <div class="d-flex justify-content-between mt-4">
                    <!-- Chỉnh sửa sản phẩm -->
                    <a href="/edit/<%= products.masp %>" class="btn btn-primary">
                        <i class="fa-solid fa-pen-to-square"></i> Chỉnh sửa
                    </a>

                    <!-- Xóa sản phẩm -->
                    <form method="POST" action="/delete/<%= products.masp %>" style="display:inline;">
                        <button type="submit" class="btn btn-danger" onclick="return confirm('Bạn có chắc chắn muốn xóa sản phẩm này?');">
                            <i class="fa fa-trash" aria-hidden="true"></i> Xóa sản phẩm
                        </button>
                    </form>

                    <!-- Quay lại danh sách sản phẩm -->
                    <a href="/listproduct" class="btn btn-secondary">
                        <i class="fa fa-arrow-left" aria-hidden="true"></i> Quay lại danh sách sản phẩm
                    </a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
